import React from "react";

const Header = () => {
  return (
    <header className="h-32  flex justify-center items-center bg-gray-100">
      <h2 className="md:text-[50px] text-[40px] font-medium text-gray-800">
        Header Section
      </h2>
    </header>
  );
};

export default Header;
